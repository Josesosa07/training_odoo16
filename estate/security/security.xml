<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="base.module_category_real_estate_brokerage" model="ir.module.category">
        <field name="description">Helps you manage your employees.</field>
        <field name="sequence">9</field>
    </record>

    <record id="estate_group_user" model="res.groups">
        <field name="name">Agent</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"/>
        <field name="comment">Real estate agents can manage the properties under their care, or properties which are not specifically under the care of any agent.</field>
    </record>

    <record id="estate_group_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="comment">Real estate managers can configure the system (manage available types and tags) as well as oversee every property in the pipeline.</field>
        <field name="category_id" ref="base.module_category_real_estate_brokerage"/>
        <field name="implied_ids" eval="[(ref('estate_group_user'))]"/>
    </record>

    <record id="limit_agents_rule" model="ir.rule">
        <field name="name">Limit agents to only being able to see or modify properties which have no salesperson, or for which they are the salesperson</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="perm_read" eval="1"/>
        <field name="groups" eval="[Command.link(ref('estate_group_user'))]"/>
        <field name="domain_force">[
            '|', ('user_id', '=', user.id),
                ('user_id', '=', False)
        ]</field>
    </record>

    <record id="admin_rule" model="ir.rule">
        <field name="name">Managers Can do everything</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="groups" eval="[Command.link(ref('estate_group_manager'))]"/>
        <field name="perm_read" eval="1"/>
        <field name="perm_write" eval="1"/>
        <field name="perm_create" eval="1"/>
        <field name="perm_unlink" eval="1"/>
        <field name="domain_force">
            [(1, '=', 1)]
        </field>
    </record>

    <record id="company_rule" model="ir.rule">
        <field name="name">Agent only can access to its company</field>
        <field name="model_id" ref="estate.model_estate_property"/>
        <field name="domain_force">
            ['|', ('company_id', '=', False),
                ('company_id', 'in', company_ids)]
        </field>
    </record>
</odoo>
